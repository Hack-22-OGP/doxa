import index from '../index'

describe('test store', () => {
  it('should return empty object', () => {
    expect(index).toEqual({})
  })

  it('test Ci', () => {
    expect(true).toBeFalsy()
  })
})
